<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- 사용자 -->
    <PreferenceCategory android:title="👤 사용자">
        <Preference
            android:key="profile"
            android:title="프로필 관리"
            android:summary="닉네임, 프로필 사진 변경"/>

        <Preference
            android:key="account"
            android:title="계정 관리"
            android:summary="비밀번호 변경, 로그아웃"/>
    </PreferenceCategory>

    <!-- 앱 설정 -->
    <PreferenceCategory android:title="⚙️ 앱 설정">
        <SwitchPreferenceCompat
            android:key="notifications"
            android:title="알림 설정"
            android:summary="푸시 알림 받기"
            android:defaultValue="true" />

        <ListPreference
            android:key="theme"
            android:title="테마 변경"
            android:summary="라이트/다크 모드 선택"
            android:dialogTitle="테마 선택"
            android:entries="@array/theme_options"
            android:entryValues="@array/theme_values"
            android:defaultValue="light"/>
    </PreferenceCategory>

    <!-- 앱 정보 -->
    <PreferenceCategory android:title="ℹ️ 정보">
        <Preference
            android:key="about"
            android:title="앱 정보"
            android:summary="버전, 개발자 정보"/>
    </PreferenceCategory>

</PreferenceScreen>
